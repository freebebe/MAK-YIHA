import { Coumponent, useState } from "react";
import ReactDOM from "react-dom";

const Todo = () => {
  const [items, setItems] = useState([]);
  const [text, setText] = useState("");

  const handleSubmit = ((e) => {
    e.preventDefault();
    !;
  });

  const newItem = () => {
  };
};
